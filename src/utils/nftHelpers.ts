import { PokeMoonNft } from "nft-uikit/constants/types"

/**
 * Returns a PokemoonNft equivalent to the one represented in the tokenUri response.
 */
export const getNftFromUriRes: PokeMoonNft = (uriRes: any) => {
    // TODO:
    let nft: PokeMoonNft = {};
    return nft;
}